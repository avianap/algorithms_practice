from collections import defaultdict
# dictionary that automatically creates an item when you try to access it
# input can be either int or list, newly create items will be initialized to either 0 or a blank list respectively
#   x = defaultdict(list)
#   x['a']
#   x
#   >>> defaultdict(<type 'list'>, {'a': []})


